/**
 * mul- multiplies two numbers
 * @a: the first number we get from the main
 * @b: the second number we get from the main
 *
 *
 * Return: returns the sum of the numbers
 */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
